#include "main.h"

/**
 * _abs - function to find the absolute value of an integer
 *
 *@n: int to be checked
 *
 *Return: the computed absolute value of an integer
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
